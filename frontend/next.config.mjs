/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "s.gravatar.com",
      "lh3.googleusercontent.com",
      "github.com",
    ],
  },
};

export default nextConfig;
